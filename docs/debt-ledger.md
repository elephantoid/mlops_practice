# 이해 부채 원장 (Comprehension Debt Ledger) — ChurnWatch

> `AGENTS.md`는 **만들 것**을, `STATUS.md`는 **만들어진 것**을 적는다.
> 이 파일은 **왜 이 모양인지 내가 설명하지 못하는 것**을 적는다.

## 이 파일의 경계

| 파일 | 축 | 답하는 질문 |
|---|---|---|
| `STATUS.md` | 프로젝트 상태 | 무엇이 만들어졌나 |
| `AGENTS.md` | 계획 | 무엇을 만들기로 했나 |
| **이 파일** | **내 이해 상태** | **내가 무엇을 설명하지 못하나** |

`STATUS.md`는 스스로를 "이 레포에서 상태를 기록하는 유일한 파일"이라 선언한다. 이 원장은 그 선언을 깨지
않는다 — 기록 대상이 프로젝트가 아니라 **나**다. 운영 규칙: **`STATUS.md`가 답할 수 있는 문장은 여기 쓰지
않는다.** 무엇이 shipped인지 적고 싶어지면 그건 `STATUS.md` 줄이다.

---

## 부채에는 두 종류가 있고, 상환 방법이 다르다

| | 결정 부채 | 메커니즘 부채 |
|---|---|---|
| 질문 | 왜 이 구조인가 / 왜 다른 게 아닌가 | 이 줄이 왜 이렇게 동작하나 |
| 답이 레포에 있나 | **없다.** 기각된 대안은 대개 기록되지 않는다 | 있다. 이 레포는 docstring에 이유를 적어둔다 |
| 상환 방법 | **재결정** — 대안을 직접 재구성하고 채택하거나 번복한다 | 읽고, 코드 없이 설명한다 |
| 왜 어려운가 | **하지 않은 추론은 회상할 수 없다** | 시간만 쓰면 된다 |

핵심은 이거다. 이 레포는 이유를 친절하게 적어놨기 때문에 **읽으면 납득된다.** 그런데 납득은 *동의*이지
*결정*이 아니다. "왜 이렇게 했냐"에 "읽어보니 타당해서요"는 답이 되지 않는다. 결정 부채는 기억해내서
갚는 게 아니라, 대안을 직접 세우고 판정해서 갚는다. 번복하면 그건 이제 내 결정이고, 채택해도 내 결정이다.
어느 쪽이든 "시키는 대로 했다"에서 벗어난다.

**그래서 결정 부채가 1순위다.** 면접에서 파고드는 것도, 지금 답 못 하는 것도 이쪽이다.

## 작성 규칙

- **쓰기 트리거는 "테스트 실패"가 아니라 "설명 불가"다.** 초록불은 신호가 아니다.
- **소스는 `파일:심볼`까지.** `/learn-review` 에이전트가 Read/Grep으로 이 경로를 다시 연다.
- **등급은 잠정.** 확정 선별은 `debt-triage`가 한다.
- **`후보`는 본인 확인 전이다.** 확인 없이 올리지 않는다 — 이미 설명 가능한 걸 부채로 적는 순간
  원장이 거짓말을 시작하고, 그때부터 이 파일은 읽히지 않는다.
- **한 줄 = 한 질문.** 파일 이름을 적고 있으면 쪼갠다.

## 의식: 예측 후 실행 (매 세션)

이 원장이 채워지는 속도보다 중요한 건 **오차 신호를 만드는 것**이다. 에이전트에게 뭘 시키면
결과가 나오고, 테스트가 통과하고, 아무도 반박하지 않는다 — 틀릴 기회가 없으면 모델은 생기지 않는다.
예전 업무에서 시스템 모델을 만들어준 건 지식이 아니라 **결과를 맞은 경험**이었다. 여기엔 그게 없으므로
인공적으로 만든다.

**규칙 1 — 실행 전에 두 줄을 먼저 쓴다.** 무엇이 일어날지, 왜 그렇게 생각하는지.
그다음 실행한다. **맞히는 게 목적이 아니라 빗나가는 게 목적이다.** 크게 빗나간 예측일수록 큰 신호다.
빗나갔으면 그 자리에서 결정 부채 또는 메커니즘 부채 한 줄로 내려 적는다.

**규칙 2 — 주 1회는 고의로 부순다.** 한 군데 바꾸고 → **어느 테스트가 어떤 메시지로 죽을지 먼저 적고**
→ 돌리고 → 되돌린다. 선례가 이미 레포 안에 있다: `tests/test_skew.py` docstring은
`serialization_alias="MonthlyCharges"` → `"MonthlyCharge"` 변이로 이 테스트가 실제로 깨지는 것을
확인했다고 기록한다. "이 호출의 파생 영향"에 대한 감각은 읽어서 생기지 않고 흔들어야 생긴다.

**규칙 3 — 지시하지 말고 판정한다.** 선택지를 만들 수 없는 영역에서도 *고를* 수는 있다.
에이전트에게 대안 2개와 트레이드오프를 내놓게 하고, 근거를 붙여 고른다. 고른 것이 내 결정이 된다.
결정 부채의 상환은 이 형태로만 일어난다.

**목표 높이.** 스택 전체를 아는 것이 아니라 **경계 모델** — 각 컴포넌트의 책임, 이음새에서 오가는 것,
깨졌을 때 번지는 범위. 아래 결정 부채가 대부분 이음새 위에 있는 것은 우연이 아니다.

---

## 1. 결정 부채 (1순위)

상환 = 대안 재구성 → 채택 또는 번복 → 근거 한 줄을 이 표의 "지금 할 수 있는 답"에 덮어쓰기.
상태: `미재결정` → `재결정(채택)` / `재결정(번복)`.

### 1-A. 결정표에 적혀 있지만 근거가 시장 신호인 것들

`AGENTS.md`의 결정표는 기각된 대안까지 적어둔 좋은 문서다. **그런데 이유 칸을 다시 읽어보면
"4/9 JDs", "70%+ Korean DS JDs" 처럼 채용공고 빈도가 근거다.** 그건 *배울 것*을 고르는 근거로는
타당하지만 *설계*의 근거는 아니다. 면접관은 후자를 묻는다. 결정표가 존재한다는 사실이 방어가 되지 않는다.

| 결정 질문 | 층위 | 등급 | 지금 할 수 있는 답 | 상환 행동 | 소스 | 상태 |
|---|---|---|---|---|---|---|
| 왜 Airflow인가, 왜 Prefect가 아닌가 | 오케스트레이션 | 🔴 | **2026-09-22 판정: Airflow 채택, 근거 교체.** "JD 빈도"가 아니라 **"이 도구가 강제하는 제약이 내가 배워야 할 개념"**이다 — 별도 프로세스, XCom 크기 한계, backfill 의미론, 최상단 파싱 함정은 어느 오케스트레이터로도 전이된다. Prefect는 그걸 가려서 덜 가르친다. **옛 근거 "4/9 JDs"는 9건 표본(2026-07-29, 원본 소실)이고, career/ 222건 재분석에서 Airflow는 코어 78건 중 6%·하락으로 확인됐다 — JD 근거로 골랐다면 오늘 무너졌을 자리** | 이 근거에서 DAG 설계 규칙을 도출할 것: 태스크는 얇게·로직은 `src/`에·데이터는 XCom이 아니라 스토리지로 | `AGENTS.md` 결정표(개정 완료), `~/Documents/career/Phase1_재분석_채용공고222건_2026Q4.md` §4 | 재결정(채택) |
| 왜 GCP Cloud Run인가 — 그리고 **지금 할 거긴 한가** | 배포 | 🔴 | **2026-09-22 판정(같은 날 번복): 진행.** 처음엔 비용을 이유로 연기했으나, 그 근거가 JD 숫자 5배 오류가 드러난 `blueprint/` 문서였다. 실제 요금을 확인하니 — **스케일 투 제로, 유휴 무과금**(min-instances>0일 때만 과금), 무료 월 180K vCPU-초·360K GiB-초·200만 요청. 봇 스캔 포함해도 근처도 못 간다. **실제 청구서는 Artifact Registry 0.5GB 초과분뿐** — sklearn·LightGBM·MLflow·pyarrow 런타임 이미지 하나로 넘긴다. 즉 비용은 요금제 문제가 아니라 **이미지 크기라는 엔지니어링 과제**다. 더해 Q4 플랜 DoD가 '공개 URL 75일+ 가동'을 요구해 10월 초에 시계가 돌아야 하므로, 연기는 그 DoD를 불가능하게 만든다 | W3(10/5~10/11) 배포. 이전 시 콜드스타트 대 모델 로딩 시간·이미지 크기를 실측해 결정표 근거를 교체 | `STATUS.md` "What has to be true before the Cloud Run move", cloud.google.com/run/pricing | 재결정(진행) |
| 왜 LightGBM인가 — 이 데이터셋에서 실제로 이겼나 | 모델 | 🟡 | "JD 70%, 빠르고 SHAP 가능" | 7,043행 × 21피처는 GBM이 로지스틱을 크게 못 이기는 규모다. 베이스라인 대비 AUC 차이를 **숫자로** 확인하라. 차이가 작으면 그 사실 자체가 더 좋은 면접 답변이다 | `AGENTS.md` 결정표, `src/models/train.py` | 미재결정 |
| Evidently를 골랐다면서 왜 Grafana도 돌고 있나 | 모니터링 | 🟡 | 없음 — 결정표는 "Grafana+custom 기각"인데 PR #3이 Prometheus+Grafana를 넣었다 | 둘이 **다른 일**을 한다는 걸 한 줄로 설명하라(배치 드리프트 리포트 vs 실시간 서비스 메트릭). 설명이 안 되면 결정표가 틀린 것이니 고칠 것 | `AGENTS.md` 결정표, `STATUS.md` M5 항목, `src/monitoring/drift.py` | 미재결정 |
| 왜 Kubernetes를 안 쓰나 | 인프라 | 🟢 | "1인 프로젝트에 Cloud Run이면 충분" — 아마 그대로 방어 가능 | 방어 가능하면 `재결정(채택)`으로 올리고 닫아라 | `AGENTS.md` 결정표 | 미재결정 |

### 1-B. 빌드 중에 내려진 결정 (`STATUS.md`의 divergence)

`STATUS.md`는 계획에서 벗어난 지점을 정직하게 기록해뒀다. **각 항목은 누군가 판단을 내린 자리이고,
그 누군가가 내가 아니었다.**

| 결정 질문 | 층위 | 등급 | 지금 할 수 있는 답 | 상환 행동 | 소스 | 상태 |
|---|---|---|---|---|---|---|
| 왜 배포(M3)를 미루고 로컬 관측성을 먼저 했나 | 순서 | 🔴 | **2026-09-22 판정: 채택.** 비용 없이 파이프라인을 완성한 뒤 배포로 검증하는 순서가 맞다. PR #3의 순서 변경을 내 결정으로 승계한다 | 없음 — 닫힘. 다만 M3 복귀 시점(로컬 파이프라인 완성)을 `STATUS.md`에 남길 것 | `STATUS.md`, PR #3 | 재결정(채택) |
| 왜 개발을 Linux 컨테이너 안으로 옮겼나 | 개발환경 | 🟡 | "libomp/libgomp 분기 때문" — 인용은 되지만 재현은 못 함 | 호스트 `.venv`의 macOS wheel이 먼저 잡히는 실패를 **한 번 재현**하라. 그게 `/opt/venv`를 마운트 밖에 둔 이유 전체다. 재현 못 하면 그 설계를 설명할 수 없다 | `CLAUDE.md` 환경 노트, `docker-compose.yml:dev`, `Dockerfile` dev 스테이지 | 미재결정 |
| nginx ingress를 왜 기각했고, **언제 필요해지나** | 인프라 | 🟢 | 기각 사유는 적혀 있음(TLS 없음·static 없음·두 번째 백엔드 없음) | 기각은 방어 가능하다. 역방향을 답할 수 있어야 완결된다 — 셋 중 무엇이 생기면 ingress가 필요해지는가 | `STATUS.md` | 미재결정 |

### 1-C. 코드 구조 자체 — 아무도 "왜"를 묻지 않은 것들

| 결정 질문 | 층위 | 등급 | 지금 할 수 있는 답 | 상환 행동 | 소스 | 상태 |
|---|---|---|---|---|---|---|
| 왜 `main.py`가 `train.py`를 import하지 않고 `MODEL_URI`를 **복제**하나 | 모듈 경계 | 🔴 | 없음 | 이건 **DRY를 의도적으로 깬 자리**다. import하면 서빙 이미지에 sklearn·LightGBM이 딸려 들어온다. "중복이 결합보다 싼 경우"를 일반화해서 말할 수 있어야 한다 — 아키텍처 질문의 정석 | `src/api/main.py:MODEL_URI` 주석, `src/models/train.py` | 미재결정 |
| 왜 전처리를 모델 아티팩트 **안**에 넣었나 (sklearn Pipeline) | 학습/서빙 경계 | 🔴 | 없음 | 대안은 서빙 코드에서 전처리하기. 아티팩트 안에 넣은 선택이 training/serving skew의 **근본 처방**이고, `test_skew.py`는 그 처방이 새는 좁은 틈만 지킨다. 이 둘의 관계를 설명하라 | `src/features/pipeline.py:build_pipeline`, `src/api/main.py:FEATURE_COLUMNS` | 미재결정 |
| `${PWD}` 절대경로 마운트는 **배포를 막는 결정 아닌가** | 아티팩트 저장 | 🔴 | **2026-09-22 판정(같은 날 갱신): 트리거 ① 발화 — 연기 종료.** MLflow는 experiment 생성 시점에 절대 `artifact_location`을 DB에 박는다(런 0개인 현재 DB에도 이미 들어있음을 측정 확인). Cloud Run에는 미러링할 호스트 경로가 없으므로 registry 경로는 따라갈 수 없다. **답은 열린 선택이 아니라 강제된다 — C안(이미지에 굽기).** 다행히 `src/models/export.py`로 이미 구현돼 있고 `docker run`이 이미 그 경로를 쓴다 | 완성 조건에 구운 경로를 넣어둔 어제 판정이 여기서 값을 한다 — 이전이 재설계가 아니라 배포가 된다. 로컬 개발은 A안(절대경로) 유지, 배포는 C안 — 코드 경로는 `load_model()` 하나로 동일 | `docker-compose.yml` mlruns 마운트 주석, `src/models/export.py`, `STATUS.md` | 재결정(채택: C안) |
| Pushgateway를 왜 쓰나 — 공식 문서는 대개 쓰지 말라고 한다 | 관측성 | 🟡 | "드리프트가 배치 잡이라 스크랩할 타깃이 없어서" | Prometheus 문서가 드는 **예외 케이스**에 이게 해당하는지 확인하라. 해당하면 근거가 단단해지고, 아니면 대안(textfile collector, 리포트를 파일로만)을 검토하라 | `docker-compose.yml:pushgateway`, `src/monitoring/drift.py` | 미재결정 |
| 로컬과 배포가 **서로 다른 모델 소스**를 써도 되는가 | 학습/서빙 경계 | 🟡 | **2026-09-22 판정: 달라도 된다 — 조건부.** `load_model()`이 `models:/`와 평면 경로를 모두 처리하므로 **코드 경로는 동일하고 설정(`MODEL_URI`)만 다르다.** 환경별 설정 차이는 12-factor이고 정상. (초기에 "테스트한 것과 배포한 것이 달라진다"는 반론이 있었으나 코드 경로가 같다는 점에서 기각) | **조건: 양쪽이 같은 아티팩트여야 한다.** 지금은 `export.py`가 registry에서 꺼내므로 성립. 이 연결이 끊기는 경우(수동 복사, 재빌드 누락)를 무엇으로 막을지 정할 것 — 못 막으면 기각했던 반론이 그때 진짜가 된다 | `src/api/main.py:load_model`, `src/models/export.py`, `Dockerfile` | 재결정(채택) |
| 왜 sqlite인가, 왜 Postgres가 아닌가 | 저장소 | 🟡 | 없음 | 지금은 맞는 선택일 가능성이 높다. 다만 **언제 틀려지는지**(동시 쓰기, Airflow 메타DB 합류)를 말할 수 있어야 한다 | `docker-compose.yml:mlflow`, `CLAUDE.md` 환경 노트 | 미재결정 |
| 왜 Telco Churn 데이터셋인가 | 포트폴리오 전략 | 🔴 | **2026-09-22: 이미 답이 있었다 — 교체 확정.** Q4 플랜이 이 레포를 `riskwatch`(신용 Home Credit + 이상거래 IEEE-CIS)로 재타겟·최우선 승격한다. 근거는 222건 재분석에서 확인된 토스증권 FDS 공고가 JJ 경력과 거의 1:1이라는 것, 그리고 데이터 특성(극단 불균형·비용 비대칭·설명가능성·드리프트)이 공유된다는 것. **오늘 아침의 W1D1 pytest가 그 재타겟의 첫 단계였다** | 재타겟이 `AGENTS.md`/`STATUS.md`에 아직 반영되지 않았다 — 반영할 것 | `~/Documents/career/AI엔지니어_전환_마스터플랜_2026Q4.md`, `Phase1_재분석_채용공고222건_2026Q4.md` | 재결정(채택) |

---

## 2. 메커니즘 부채 (2순위)

상환 = 코드를 열지 않고 ① 무엇을/왜 ② **왜 다른 방법이 아닌지** 둘 다 말하기. ②가 없으면 암기다.
상태: `후보` → `미상환` → `상환`.

| 개념 | 트랙 | 등급 | 소스 | 상태 | 캡처일 | 비고(혼란도) |
|------|------|-----|------|------|--------|-------------|
| Registry와 아티팩트의 차이 — 포인터 대 바이트 | 데이터/저장소 | 🔴 | `mlflow.db:experiments.artifact_location`, `src/api/main.py:load_model`, `src/models/export.py` | 미상환 | 2026-09-22 | **예측 빗나감(2026-09-22).** `artifact_location`에 `models:/churnwatch@production`이 있을 거라 예측했으나 실제는 절대 파일경로였다. registry는 바이트를 갖지 않고 **포인터만** 갖는다 — `models:/` → 별칭 → 버전 → run → 절대경로의 4홉 해석. 예측한 건 1홉, DB에 있는 건 마지막 홉. `${PWD}` 마운트가 필요한 이유 전체가 여기서 나온다. 설명은 들었으나 코드 없이 재구성은 아직 |
| `16 passed, 1 skipped`가 실제로 보증하는 범위 | 테스트전략 | 🔴 | `tests/test_skew.py::registered_model` | 후보 | 2026-09-21 | 나머지 16개는 전부 `StubModel` 상대다. 실제 모델을 두 경로로 채점해 skew를 잡는 단 하나의 테스트가 오늘 skip됐다. 초록불이 보증하는 건 요청/응답 계약이지 서빙 정확성이 아니다 |
| `reindex(columns=FEATURE_COLUMNS)`의 양날 | 데이터계약 | 🔴 | `src/api/main.py` predict 핸들러, `tests/test_skew.py` docstring | 후보 | 2026-09-21 | 순서는 고정하지만 못 찾은 컬럼을 NaN으로 채운다 → alias 오타 한 글자가 조용히 imputation으로 흡수된다 |
| 결정 임계값을 아티팩트가 아니라 서빙에 둔 선택 | 서빙/설계 | 🔴 | `src/api/main.py:DECISION_THRESHOLD` | 후보 | 2026-09-21 | 재학습 없이 결정경계를 옮기기 위해서다. 역으로 **임계값이 모델 안에 들어가야 하는 경우**까지 말할 수 있어야 상환 |
| `serialization_alias` vs `alias` | 데이터계약 | 🟡 | `src/api/schemas.py` docstring | 후보 | 2026-09-21 | `alias`였다면 CamelCase가 *입력* 스펠링이 되고 OpenAPI에도 샌다 |
| `Literal` 집합 ↔ `ingest.py`의 `isin` 이중 정의 | 데이터계약 | 🟡 | `src/api/schemas.py:YesNo*`, `src/data/ingest.py` | 후보 | 2026-09-21 | 갈라지면 미학습 카테고리가 -1로 인코딩되어 자신만만한 예측이 나온다. 강제하는 테스트가 없다 |
| 히스토그램 버킷이 곧 분해능 | 관측성 | 🟡 | `src/api/main.py:LATENCY_BUCKETS` | 후보 | 2026-09-21 | 기본 버킷은 5ms 시작 → 3~6ms 서비스의 모든 관측치가 첫 버킷에 들어가고 분위수가 무의미해진다 |
| 메트릭 라벨 카디널리티 | 관측성 | 🟡 | `tests/test_api.py::test_unmatched_paths_share_one_metric_label` | 후보 | 2026-09-21 | 원시 path 라벨링은 스캐너가 긁는 URL마다 시계열을 만든다 |
| MLflow alias vs stage, sqlite 절대경로 | 도구/환경 | 🟡 | `src/api/main.py:load_model`, `CLAUDE.md` | 후보 | 2026-09-21 | 셋이 각각 다른 실패다 — stages deprecated(2.9) / `file:` 백엔드 거부(3.x) / CWD 상대경로는 빈 DB를 조용히 만든다 |
| StarletteDeprecationWarning (httpx → httpx2) | 도구/환경 | 🟡 | pytest 출력, `fastapi/testclient.py:1` | 후보 | 2026-09-21 | 무시해도 되는 근거와 언제 실패로 바뀌는지를 모른다. 경고를 "원래 나는 것"으로 넘기면 경고가 신호이길 그만둔다 |
| pytest-asyncio STRICT가 unpinned | 도구/환경 | 🟡 | `pyproject.toml`의 `[tool.pytest.ini_options]` 부재 | 후보 | 2026-09-21 | 동작은 맞지만 라이브러리 기본값에 의존 중. 판단인지 누락인지부터 |
| 메트릭을 모듈 레벨에 정의하는 이유 | 관측성 | 🟢 | `src/api/main.py:REQUESTS` | 후보 | 2026-09-21 | 핸들러/팩토리 안이면 두 번 등록되어 `DuplicateTimeseries` |
| 테스트 가능성이 설계를 규정한 사례 | 테스트전략 | 🟢 | `src/api/main.py:load_model` ↔ `tests/test_api.py::client` | 후보 | 2026-09-21 | lifespan에 인라인했으면 스텁 꽂을 지점이 없다. "모듈 레벨 함수"가 스타일이 아니라 계약인 경우 |
| 관측성은 degrade하되 서빙을 죽이지 않는다 | 서빙/설계 | 🟢 | `tests/test_api.py::test_logging_failure_does_not_break_prediction` | 후보 | 2026-09-21 | 로깅 실패가 500을 만들면 "관측 추가"가 곧 "가용성 하락"이 된다 |

---

## 2-B. 재도출 부채 — Telco에서 측정된 숫자들 (2026-09-22 추가)

riskwatch 재구성으로 **수치 자체가 무효가 된** 상수들이다. 코드에는 남아 있고 동작도
하지만, 근거가 사라진 숫자다. 도메인이 바뀌었으니 재측정 전까지는 "임의의 값"과 구별되지
않는다.

| 상수 | 위치 | Telco 측정치의 출처 | 왜 무효인가 | 재도출 시점 |
|---|---|---|---|---|
| `drift_share > 0.2` | `.claude/rules/monitoring.md:39`, Grafana 빨간 밴드 | 19개 피처 중 1개 드리프트 = 0.0526 기준으로 잡은 값 | 신용 트랙은 모델링 피처가 26개, 사기 트랙은 30개. 분모가 다르면 같은 임계가 다른 사건을 가리킨다 | W3 Step 17 |
| `AUC delta > 0.01` | 승격 게이트 | Telco ROC-AUC 분포에서 "의미 있는 개선"으로 잡은 폭 | 양성률 0.173%에서 ROC-AUC 자체가 약한 지표다. W2에서 선택 지표가 PR-AUC로 바뀌면 이 델타는 다른 축의 숫자가 된다 | W3 Step 17 |
| `MIN_CURRENT_ROWS = 100` | `src/monitoring/drift.py:94` | 40행 → drift_share 1.0, 300행 → 0.0 관측에서 잡은 하한 | 같은 실험을 신용/사기 데이터로 다시 해야 한다. 리뷰 3패스 중 2패스가 이 상수를 놓쳤다 — 앞의 두 개만 부채로 적혀 있었기 때문 | W3 Step 17 |

**세 개를 함께 적어 두는 이유:** 앞의 두 개는 계획 단계에서 부채로 잡혔지만
`MIN_CURRENT_ROWS`는 리뷰 1패스의 Critic이 찾아냈다. "측정된 숫자"라는 같은 범주인데
하나만 빠져 있었던 것 — 범주로 묶어 두지 않으면 다음에도 같은 식으로 빠진다.

## 2-C. 시뮬레이션 면허의 확장 (2026-09-22)

`AGENTS.md`의 Drift Simulation 절은 `MonthlyCharges * 1.15`를 "문서화된 시뮬레이션
표준"으로 허용한다. 이번 재구성에서 이 면허를 **트랙별 컬럼 선택**으로 넓혔다
(`SYNTHETIC_DRIFT_COLUMN`: 신용 `AMT_CREDIT`, 사기 `Amount`).

- **넓힌 것:** 어떤 컬럼을 흔들지가 도메인별로 달라진다.
- **넓히지 않은 것:** 여전히 **실데이터 위에** 적용하는 재현 가능한 교란이다. 기저
  데이터 합성이나 클래스 불균형 조작은 면허 밖이며, 그렇게 하면 DoD ④⑤가 순환 논증이
  된다 — 임계를 정하는 사람이 분포도 정하는 꼴이다.

상속이 아니라 **날짜가 붙은 결정**으로 적는다. 원래 면허가 한 컬럼짜리였다는 사실이
남아야 다음 확장이 또 슬쩍 넘어가지 않는다.

---

## 3. 상환 완료

| 항목 | 종류 | 상환일 | 상환 근거(어떻게 검증했나) |
|------|------|--------|--------------------------|
| _(아직 없음)_ | | | |
